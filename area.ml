let surface_area: int = 
let length = 8 in
let height = 9 in 
let width = 10 in
let base_area = length * width in
let side_area = length * height in
let top_area = height * width in
  2 * (base_area + side_area + top_area)


;;print_int (surface_area)